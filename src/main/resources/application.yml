spring:
  datasource:
    # Docker Compose ??? ?? 'db'? ????? ??
    url: jdbc:mysql://localhost:3306/mydb?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Seoul
    username: root # docker-compose.yml? MYSQL_USER ?? MYSQL_ROOT_PASSWORD? ??
    password: 1234 # docker-compose.yml? MYSQL_PASSWORD ?? MYSQL_ROOT_PASSWORD? ??
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      # 'update' : ?? ? ??? ???? @Entity?? ??? ?????? ??? ??(??? ?? X)
      ddl-auto: update
    show-sql: true          # ???? SQL ??? ??? ??
    properties:
      hibernate:
        format_sql: true   # SQL ??? ???? ?? ?? ??
        dialect: org.hibernate.dialect.MySQLDialect
    defer-datasource-initialization: true   # ??????(JPA)? DDL(???) ??? ??? ?, data.sql? ????? ??

  # src/main/resources/??? data.sql? ?? ? ?? ??? SQL ??? ???? ?? ??
#    sql:
#        init:
#            mode: always

logging:
  level:
    org:
      springframework:
        security: DEBUG

# =============== ?? ??? ?? ===============
# ????? ?? ???
server:
  servlet:
    encoding:
      charset: UTF-8  # ?? ??? ??? ?? ?? ???? UTF-8? ??
      enabled: true     # ? ??? ??? ???
      force: true       # ?? ? ???? ??? ??